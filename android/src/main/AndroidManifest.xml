<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  package="com.github.florent37.assets_audio_player">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application>
        <receiver android:name=".notification.NotificationActionReceiver" android:exported="false" tools:node="merge"/>
        <receiver android:name=".notification.CustomMediaButtonReceiver" android:exported="false" tools:node="merge">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <service
            android:name=".notification.NotificationService"
            android:enabled="true"
            android:exported="true">
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
            -->
        </service>

    </application>

</manifest>
